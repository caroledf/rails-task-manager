  <h2><%= @task.title %></h2>
  <p><%= @task.details %></p>

  <% if @task.completed %>
    <input type="checkbox" checked> The task is completed<br>
  <% else %>
    <input type="checkbox"> The task is not completed<br>
  <% end %>

<%= link_to "Back to list", tasks_path %>


